name: Build and Push Ollama Image

on:
  push:
    branches:
      - main
    paths:
      - 'ollama/**'
      - '.github/workflows/build-push-ollama.yml'

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./ollama
          push: true
          platforms: linux/amd64
          tags: |
            schnicklbob/llm-voice-ollama:latest
            schnicklbob/llm-voice-ollama:${{ github.sha }}
          cache-from: type=gha,scope=ollama
          cache-to: type=gha,mode=max,scope=ollama
